apply plugin: 'java'
apply plugin: 'application'

sourceSets {
    main {
        java {
            srcDirs = ["src"]
        }
    }
}

mainClassName = 'NetTopology'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.prefuse:prefuse:beta-20071021'
}
